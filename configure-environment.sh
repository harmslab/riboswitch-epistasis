# assumes a basic scientific computing environment with
# matplotlib scipy tqdm numpy pandas

# only non-standard dependency is the likelihood library:
git clone git@github.com:harmslab/likelihood.git
cd likelihood
python setup.py install
